"""Unit tests for deterministic business logic (strength parsing, synonym mapping, unit
conversion, change classification thresholds, savings math, safety rules R1-R6).

TODO(D2/D3): add tests here as `services/*.py` and `agents/safety_agent.py` are implemented -
see backend.instructions.md "Testing" and implementation-plan.md Section 6 (test matrix).
"""


def test_placeholder() -> None:
    """Keeps this package collectible by pytest until real unit tests land."""
    assert True
