# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: health-iq-hackathon
metadata:
  template: health-iq-hackathon@0.1.0
infra:
  provider: bicep
  path: infra
  module: main
# Container Apps hosting (backend/frontend) is optional and disabled by default
# (infra/main.bicep `deployContainerApps` parameter). Local development runs the
# backend/frontend directly on your machine against the same provisioned Azure resources -
# see infra/README.md.
